spring:
  # JPA properties
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  # Flyway
  flyway:
    enabled: false
  datasource:
    password: admin
    username: postgres
    url: jdbc:postgresql://localhost:5432/queue
security:
  secret:secret